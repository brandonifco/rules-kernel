# Calibration for 0.4.0

Runs of `tools/calibration/run.sh` against the consumers pinned in
`tools/calibration/consumers.json` ([decision 0021](../decisions/0021-external-calibration-is-a-release-gate.md)).
If `src/RulesKernel/` or `src/RulesKernel.Analyzers/` changes again before the tag,
`tools/release-checks.py` rejects this record until it is rerun.

**Fidelity.** These runs used SDK 10.0.111 in place of the consumers' pinned 10.0.112, and SDK
8.0.130 in place of SRD_Combat's 8.0.129 (`CALIBRATION_SDKS`), because the pinned patch
releases were not installed on the machine. `publish.yml` reruns the kernel mode at the tag
with the pinned SDK.

## Kernel

Kernel commit: 793252e36a8a6c85444b0c3dd7698be36a45e755

What changed since 0.3.0 under `src/RulesKernel/`: the `HashDerivation` canonical form
([decision 0019](../decisions/0019-replay-identity-is-necessary-not-sufficient.md)), and
documentation on `ReplayCompatibilityIdentity`, `Resolution<T>` and `UnresolvedResult`
([decision 0018](../decisions/0018-resolution-enforces-handling-not-honesty.md)).

| consumer | commit | pins | tests | result |
|---|---|---|---|---|
| faa-part-107 | 3f11dde | 1 | 156 | pass |
| hoyle-backgammon | 5ef2097 | 2 | 552 | pass |
| srd-52-combat | 894897f | 2 | 250 | pass |

All three engines are pinned to 0.2.0 in their own repositories, so this run also covers every
change from 0.2.0 to this tree. As a control during decision 0019, a variant of the derivation
check that rejected any derivation ending in `xml` failed faa-part-107's tests under the same
script.

## Analyzer

Kernel commit: 793252e36a8a6c85444b0c3dd7698be36a45e755

No analyzer rule changed since 0.3.0, so this section is a baseline rather than a requirement.

| consumer | commit | findings by rule | total |
|---|---|---|---|
| faa-part-107 | 3f11dde | none | 0 |
| hoyle-backgammon | 5ef2097 | none | 0 |
| srd-52-combat | 894897f | none | 0 |
| SRD_Combat | 9b91ffe | RK0001 10, RK0003 1, RK0004 1, RK0007 5 | 17 |

The control draw was reported in every project of every consumer, so the zeros are results.
SRD_Combat's 17 are the post-0016 numbers README records: 10 RK0001, 5 RK0007, 1 RK0003 and
1 RK0004. That independently reproduces the manual calibration.
