; Unshipped analyzer release
; Empty between releases. A rule added here ships in the next one, and RS2008 fails the
; build on a diagnostic ID that is declared nowhere -- which is what makes adding an ID a
; deliberate act rather than a side effect.
; See docs/decisions/0011.
